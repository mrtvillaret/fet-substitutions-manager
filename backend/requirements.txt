fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.9.2
python-multipart==0.0.32
sqlalchemy>=2.0.0
# PyJWT en lloc de python-jose: és el que recomana la documentació de FastAPI
# des que python-jose va quedar sense manteniment. A més, python-jose
# arrossegava ecdsa, rsa i pyasn1, i ecdsa té una vulnerabilitat sense
# correcció disponible que aquí ni tan sols és aplicable (només fem servir
# HS256, mai signatura ECDSA).
PyJWT[crypto]==2.13.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
slowapi==0.1.9

# PDF generation
reportlab>=4.0.0

# i18n (traduccions)
babel>=2.12.0

# XML parsing (per processar horari FET)
lxml>=4.9.0

# Nota: Dependències del projecte principal que també necessita el backend
# (reutilitza core/, data/, config/ però necessita les seves llibreries)
